import '../styles/App.css';
import React from 'react';

const Splash = () => {
    return (
        <section className={ 'container splash' }>
            <h2>Welcome</h2>
        </section>
    );
}

export default Splash;